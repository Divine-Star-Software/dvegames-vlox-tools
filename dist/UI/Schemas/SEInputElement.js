import { Observable } from "@amodx/core/Observers";
export class SchemaEditorObservers {
    validate = new Observable();
    loadIn = new Observable();
}
export class SchemaEditorNodeObservers {
    validate = new Observable();
    loadIn = new Observable();
}
